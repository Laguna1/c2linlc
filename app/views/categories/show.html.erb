<p>Category show page</p>
<h1><%= @category.name %></h1>
<ul>All doctors in this category:
  <% @category.users.each do |user| %>
  <li><%= link_to user.fullname, user_path(user) %></li>
  <% end %>
</ul>
<br>
<br>
<br>
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>